# Grideditor

Block style post editor

```tsx
import { GridEditor } from "@nine-co/grideditor-core";
import "@nine-co/grideditor-core/style.css";
```

The stylesheet is required for the editor layout and controls. Define the `--nge-*` custom
properties in the consuming application to adapt the editor theme.
